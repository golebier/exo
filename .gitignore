# gitingest
digest.txt

# python
**/__pycache__

# nix
.direnv/

# IDEA (PyCharm)
.idea

# xcode / macos
*.xcuserstate
*.xcuserdata
*.xcuserdatad/
**/.DS_Store
app/EXO/build/
dist/

# rust
target/
**/*.rs.bk
*.pdb

# svelte
dashboard/build/
dashboard/node_modules/
dashboard/.svelte-kit/

# host config snapshots
hosts_*.json
.swp

# bench files
bench/**/*.json

# tmp
tmp/models
/build/exo

# build outputs
output/
*.dmg
*.sha256
*.bak

# mlx native kernel build artifacts (built opt-in via EXO_BUILD_MLX_KERNELS=1)
# — the _ext nanobind extension, kernel ops dylib, compiled metallib, cmake
# build dir
mlx_kernels/build/
**/omlx_custom_kernels/**/_ext*.so
**/omlx_custom_kernels/**/*.dylib
**/omlx_custom_kernels/**/*.metallib
/.agents
/.claude/skills
/.claude
/.codex
skills-lock.json
